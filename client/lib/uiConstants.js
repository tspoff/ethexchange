export default Object.freeze({
    SEND_TOKEN_INPUT_MESSAGE: 'Directly send a token from your address to another address.',
    APPROVAL_TOKEN_INPUT_MESSAGE: 'Approve the address <i>to be allowed</i> to send a token from your address to another address. This is important for the Exchange. When you fund the token in the exchange then it will deduct in your name the token from your address to the token address.'
});